# =============================================================================
# NissiGrid Environment Variables Template
# Copy this file to .env and modify values as needed for your system
# =============================================================================

# PostgreSQL Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=scanner
DB_USER=postgres
DB_PASSWORD=

# Optional full connection URL (overrides individual DB_* settings)
# DATABASE_URL=postgresql://postgres:@localhost:5432/scanner

# LLM Fallback (Optional)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OLLAMA_MODEL=richardyoung/olmocr2:7b-q8

# Debug & Feature Flags
DEBUG_MODE=false
